import 'package:virtual_library/models/book.dart';

class FavoritesRepository {
  Future<List<Book>> getFavorites() async {
    // access shared prefrncs and get the favorites list
    return [];
  }
}
